# Контрольная проверка Release-сборки

Дата проверки: 2026-07-21.

## Результат

Конфигурация `build_release` собрана в режиме `Release`. Компилятор принял проект с предупреждениями, превращёнными в ошибки (`/W4 /WX`). Все четыре автоматических набора тестов прошли.

```text
Test project .../work/cmake_release
    Start 1: backtest_engine
1/4 Test #1: backtest_engine .................. Passed
    Start 2: data_layer_v2
2/4 Test #2: data_layer_v2 .................... Passed
    Start 3: baseline_strategy
3/4 Test #3: baseline_strategy ................ Passed
    Start 4: instrument_specs
4/4 Test #4: instrument_specs ................. Passed

100% tests passed out of 4
```

Проверены:

- причинный порядок «сигнал по закрытому бару → вход на следующем открытии»;
- учёт стороны Bid/Ask, spread, комиссии, slippage и gap-through-stop;
- консервативное разрешение неоднозначного бара: защитный stop считается сработавшим первым;
- чтение XFBAR001/XFBAR002, контроль заголовка, размера записи, временного порядка и календарная агрегация;
- BUY/SELL state machine baseline, отсутствие преждевременного сигнала и отсутствие доступа к будущим барам;
- строгий разбор спецификаций инструментов и отсутствие дубликатов.

## Smoke test исходного GBPJPY

Файл прочитан только для проверки формата:

```text
symbol,bars,first_time,last_time,digits,point,gaps,spread_p50_points,spread_p95_points,spread_p99_points,m15_bars,m15_incomplete,h1_bars,h1_incomplete,warnings
GBPJPY,70007,1737651300,1767225300,3,0.001,76,9.00,40.00,255.00,23349,35,5839,33,0
```

Это подтверждает чтение 70 007 M5-баров без предупреждений. Временные метки соответствуют 2025-01-23 16:55:00 UTC — 2025-12-31 23:55:00 UTC.

## Smoke test спецификаций

```text
symbols,spot_fx,metals,crypto,missing_commission,missing_swap_or_funding
62,24,5,33,62,62
```

Все 62 M5-инструмента имеют контрактные параметры, однако подтверждённые брокерские комиссии и swap/funding отсутствуют. Политика конфигурации `FAIL_CLOSED` поэтому намеренно запрещает сравнительную оптимизацию и публикацию top-50 до заполнения этих полей.

## Граница проверки

Release-проверка подтверждает корректность имеющегося каркаса и его тестов. Она не является доказательством прибыльности, не подтверждает полноту дискреционной системы и не заменяет зафиксированный IS/validation/blind-OOS эксперимент.
